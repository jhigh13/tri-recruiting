# Alternative Database Configuration for Local Development

# SQLite option (no server required)
DATABASE_URL=sqlite:///./data/tri_talent.db

# PostgreSQL options (if we get Docker working)
# DATABASE_URL=postgresql://tri_user:tri_password@127.0.0.1:5432/tri_talent_db

# Connection settings
POOL_SIZE=5
MAX_OVERFLOW=10
POOL_TIMEOUT=30
